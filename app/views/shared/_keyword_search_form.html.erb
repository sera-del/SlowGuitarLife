<div class="input-group mb-2 d-flex justify-content-center">
  <%= form_with url: keyword_search_posts_path, local: true, method: :get do |f| %>
    <%= f.text_field :keyword, placeholder: "投稿名、カテゴリー" %>
    <%= f.submit "検索", class: "btn btn-primary" %>
  <% end %>
</div>
